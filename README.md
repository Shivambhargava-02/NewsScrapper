# Streamlit News Scraper

This is a simple Streamlit web app that scrapes the latest headlines from the BBC India RSS feed and lets you download the data as CSV or Excel.

## How to run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

Deploy this folder directly to GitHub and connect it to Streamlit Cloud.
